# Ferramentas locais / agente (não versionar — skills se reinstalam via npx)
.agents/
.claude/

# .specs/ FICA versionado (documentação spec-driven do projeto)

# Dependências e build (Next.js — quando houver código)
node_modules/
.next/
out/
build/
dist/
.vercel/

# Variáveis de ambiente / segredos
.env
.env*.local

# Conteúdo protegido por direito autoral (provas reais Strix/EBMSP) — NÃO versionar.
# Dados ficam no Neon; figuras são servidas localmente. Ver AD-007 / nota de copyright.
web/src/data/questoes/
web/public/questoes/
provas-fonte/

# Logs e arquivos de SO
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
